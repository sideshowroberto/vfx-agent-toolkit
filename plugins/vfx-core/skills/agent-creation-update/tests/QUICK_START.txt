AGENT-CREATION-UPDATE INTEGRATION TEST QUICK START
================================================================

LOCATION:
<workspace>\.claude\skills\agent-creation-update\tests

RUN ALL TESTS (RECOMMENDED):
    cd <workspace>\.claude\skills\agent-creation-update\tests
    python run_all_tests.py

RUN INTEGRATION TESTS ONLY:
    cd <workspace>\.claude\skills\agent-creation-update\tests
    python test_integration.py

RUN UNIT TESTS ONLY:
    cd <workspace>\.claude\skills\agent-creation-update\tests
    python run_all_tests.py --unit-only

RUN WITH VERBOSE OUTPUT:
    cd <workspace>\.claude\skills\agent-creation-update\tests
    python run_all_tests.py --verbose

EXPECTED OUTPUT:
    ======================================================================
    AGENT-CREATION-UPDATE INTEGRATION TEST SUITE
    ======================================================================

    Test 1: Complete Agent Lifecycle
      ✅ Create agent (v1.0.0)
      ✅ Validate agent (PASS)
      ✅ Update agent to v1.1.0
      ✅ Validate updated agent (PASS)
      ✅ Archive created (v1.0.0)
      ✅ Cleanup successful

    ... [5 more tests]

    ======================================================================
    TEST SUMMARY
    ======================================================================
    Total Tests: 6
    Passed: 6
    Failed: 0

    🎉 All integration tests passed!
    ======================================================================

EXIT CODES:
    0 - All tests passed
    1 - One or more tests failed
    2 - Error running tests (run_all_tests.py only)

WHAT GETS TESTED:

    Test 1: Complete Agent Lifecycle
        - Create → Validate → Update → Archive → Delete

    Test 2: Create From All Templates
        - tool-specialist, cross-tool, general-helper

    Test 3: Version Increments
        - major (1.0.0 → 2.0.0)
        - minor (2.0.0 → 2.1.0)
        - patch (2.1.0 → 2.1.1)

    Test 4: Validation Prevents Invalid Agents
        - Invalid names rejected
        - Invalid agents fail validation
        - Updates blocked on invalid agents

    Test 5: Archive Restoration Workflow
        - Create, update, rollback, restore

    Test 6: Force Overwrite Workflow
        - Duplicate detection
        - --force flag behavior

TROUBLESHOOTING:

    "Script not found" error:
        → Ensure you're in the tests/ directory
        → Check scripts/ directory exists

    "Template not found" error:
        → Check reference/agent_template.md exists

    All tests fail:
        → Check Python version (need 3.8+)
        → Verify file permissions

    Tests pass but temp directories remain:
        → Manual cleanup:
          Windows: del /s /q %TEMP%\agent_test_*
          Linux:   rm -rf /tmp/agent_test_*

REQUIREMENTS:
    - Python 3.8+
    - pytest (for unit tests only)
    - All 4 scripts in scripts/ directory
    - agent_template.md in reference/ directory

DOCUMENTATION:
    - Detailed Guide: tests/README.md
    - Implementation Summary: INTEGRATION_TEST_IMPLEMENTATION_SUMMARY.md
    - Test Source: tests/test_integration.py

================================================================
Last Updated: 2025-10-25
