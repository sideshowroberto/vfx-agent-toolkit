{
  "description": "task-observer: inject the observation-log status at session start, resume and after compaction. The python dispatcher replaces the earlier bash line: bash on a Windows PATH is the WSL stub outside Claude Code. Codex ignores plugin-bundled hooks - see GETTING-STARTED (Codex) for the repo-level form.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/skills/task-observer/scripts/observe_hook.py\"",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
